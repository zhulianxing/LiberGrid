# API

OpenAI-compatible base URL: `https://libergrid.org/v1`

Current model: `libergrid-auto`

Test key: `libergrid-test`

Endpoints:

- `GET /v1/models`
- `POST /v1/chat/completions`
- `POST /api/v1/generate`

The MVP has no paid SLA. Requests are routed to available nodes when possible.
