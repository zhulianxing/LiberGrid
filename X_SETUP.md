# LiberGrid X Setup

Official setup page:

- https://libergrid.org/x-setup
- https://libergrid.org/x-setup.json
- https://libergrid.org/x-setup.txt

## Recommended Handles

1. `@LiberGrid`
2. `@LiberGridAI`
3. `@LiberGridOrg`
4. `@LiberGridNet`

## Profile

Display name:

`LiberGrid`

Bio:

`Distributed AI compute network. token for every one. Connect local model nodes, use an OpenAI-compatible API, and help build community-owned compute.`

Website:

`https://libergrid.org`

Location:

`Global`

## Pinned Post

Introducing LiberGrid: token for every one. A distributed AI compute network where local model nodes serve developers, agents, and API users through an OpenAI-compatible API. Start here: https://libergrid.org/launch

## First Posts

1. Introducing LiberGrid: token for every one. A distributed AI compute network where local model nodes serve developers through an OpenAI-compatible API. Try it: https://libergrid.org

2. Have a spare machine running Ollama? Connect it to LiberGrid and help build a community AI compute network. Node guide: https://libergrid.org/worker

3. OpenClaw users can try LiberGrid as an OpenAI-compatible provider. Base URL: https://libergrid.org/v1 · Model: libergrid-auto · Setup: https://libergrid.org/openclaw

4. LiberGrid starts with off-chain credits and public reports. Public-chain settlement comes later, after real node growth, audits, caps, and pause controls. Roadmap: https://libergrid.org/roadmap

## Human Checklist

1. Create or log into the X account with the owner email.
2. Set display name to `LiberGrid`.
3. Choose the first available recommended handle.
4. Set website to `https://libergrid.org`.
5. Add the bio.
6. Post and pin the pinned post.
7. Send the final X profile URL back to Codex/OpenClaw so `/channels.json` can be updated.

## Safety

- Do not commit or publish the X password.
- Human owner completes any X email, phone, CAPTCHA, or 2FA verification.
- Enable 2FA after the account is created.
- Use https://libergrid.org/channels as the trust anchor.
